rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Site içeriği: herkes okuyabilir, yalnızca giriş yapmış yönetici yazabilir.
    match /site/content {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
